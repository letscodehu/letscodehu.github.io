## B2C page: checklist inline entrypoints

### Status

The standalone `TrainingB2CPage` that hosted checklist callouts and gift-box CTAs was removed. B2C conversion and schedule content now live on `TrainingB2CAdsLandingPage` (`training/workshop-budapest`). The old `trainingB2c` i18n namespace was removed with that page.

### Historical reference

Previously: `trainingB2c.problemImmediateHelp` in a problem-section callout; `trainingB2c.ctaGiftTitle` / `trainingB2c.ctaGiftBody` above the bottom CTA on the removed page.
