# Training prospectus download link

- Added a secondary CTA on `TrainingPage` that links to `/downloads/Letscode%20Solutions%20Prospectus.pdf`.
- The same English PDF asset is used for both `hu` and `en` locales.
- Added localized CTA labels via `training.prospectusCta` in `src/i18n/hu.json` and `src/i18n/en.json`.
